### 1. 파일별 기능 중복에 의한 에러 처리 오류

**1. 상황**
소수 입력값을 예외처리 하는데 의도대로 코드가 돌아가지 않는 문제 발생

**2. 분석**
코드의 순서에서 해당 부분이 의도와 달라지는 부분이 있었다. parseInt를 2번 진행하고 있었고, 에러 검사를 하기 전에 App.run()에서 먼저 parseInt를 하고 또 에러 핸들러에서 parseInt를 하고 있어서 에러 핸들러에서는 1.5가 1로 되고 있었다.

**3. 해결**
App.run()에서 parseInt 제거
